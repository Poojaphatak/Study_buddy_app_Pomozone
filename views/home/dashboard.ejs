<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pomozone</title>
  <link rel="stylesheet" href="css/dashboardPage.css">
  <link rel="stylesheet" href="css/pomodoro.css">
  <link rel="stylesheet" href="css/music.css">
  <script defer src="/frontend/music.js"></script>
</head>
<body id ="main_body" >

  <div class="overlay"></div>

  <div class="content">
    <header>
      <h1 class="logo">Pomozone</h1>
      <nav class="nav-top">
        <a href="/spaces">Home</a>
        <a href="/feedback/<%=user._id%>">Feedback</a>
        <a class="logoutBtn" href="#">Logout</a>
      </nav>
    </header>

  <div class="motivation_box hide">
    <p id="quote-box">"Don’t watch the clock; do what it does. Keep going.” — Sam Levenson",
    </p>
    <button id="quote_btn">New Quote</button>
  </div>

  
<section class="timer-app hide">
 <div class="timer-box ">
    <div class="header">
      <span>Personal</span>
      <div class="dots">
        <span>.</span><span>.</span><span>.</span><span>.</span>
      </div>
    </div>
    <div class="timer-display">
      <h1 id="time">30:00</h1>
      <button id="startStop">Start</button>
      <button id="reset" title="Reset">&#8635;</button>
    </div>
    <div class="modes">
      <button class="mode active" data-mode="pomodoro">Pomodoro</button>
      <button class="mode" data-mode="short">Short Break</button>
      <button class="mode" data-mode="long">Long Break</button>
    </div>
  </div>
</section>
 <section class="player-container hide">
    <div class="container">
      <h2>Playlist</h2>
      <ul id="song-list">
        <li data-index="0" class="active">Lofi</li>
        <li data-index="1">Rain</li>
        <li data-index="2">Waves</li>
        <li data-index="3">Delta</li>
        <li data-index="4">Forest</li>
        <script>
          document.addEventListener('DOMContentLoaded', function() {
            const songList = document.getElementById('song-list');
            songList.addEventListener('click', function(e) {
              if (e.target && e.target.nodeName === "LI") {
          Array.from(songList.children).forEach(li => li.classList.remove('active'));
          e.target.classList.add('active');
              }
            });
          });
        </script>
      </ul>

      <div id="title">Lofi</div>


      <div class="time-container">
        <span id="current-time">0:00</span>
        <span id="duration">0:00</span>
      </div>
      <input type="range" id="progress" value="0" />

      <div class="controls">
        <button id="prev">←</button>
        <button id="play">▶</button>
        <button id="next">→</button>
        <button id="loop" class="loop-on">🔁</button>
      </div>

      <audio id="audio" loop></audio>
    </div>
  </section>

    <footer>
      <nav class="nav-bottom">
        <a href="#" id="timer_tag">Pomodoro</a>
        <a href="/tasklist/<%=user._id%>">To-Do list</a>
        <a href="/progressPage/<%=user._id%>">Progress-bar</a>
        <a href="#" id="motivation_tag">Motivation!</a>
        <a id="music_btn" href="#">Music</a>
        <a href="/displayFriends">Connect With Friends</a>
      </nav>
    </footer>
  </div>
<script src="/frontend/theme.js"></script>
<script src="/frontend/musicPlayer.js"></script>
<script src="/frontend/logout.js"></script>
  <script src="/frontend/dashboard.js"></script>
  <script src="/frontend/progress.js"></script>
   <script src="/frontend/pomodoro.js"></script>

</body>

</html>
